{% if detail.article_url %}<a href="{{ detail.article_url }}" target="_blank" rel="external">{{ detail.title_of_article }}</a>{% endif %}{% if detail.article_date %} , {{ detail.article_date }}{% endif %}{% if detail.publication_url and detail.publication_name %} &mdash; <a href="{{ detail.publication_url }}" target="_blank" rel="external">{{ detail.publication_name }}</a>{% endif %}